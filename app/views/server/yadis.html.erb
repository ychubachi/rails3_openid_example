<?xml version="1.0" encoding="UTF-8"?>
<xrds:XRDS
    xmlns:xrds="xri://$xrds"
    xmlns="xri://$xrd*($v*2.0)">
  <XRD>
    <Service priority="0">
      <% @types.each do |type| %>
      <Type><%= type %></Type>
      <% end %>
      <URI><%= url_for(:controller => 'server', :only_path => false) %></URI>
    </Service>
  </XRD>
</xrds:XRDS>
